# CLAUDE.md

## 项目结构

- `cXX/` — 各章节的实验配置、脚本与答案
- `elasticsearch-course/` — 课程 Markdown 正文
- `.markdownlint-cli2.yaml` — 仓库级 Markdown 检查配置

## 提交规范

### 语言

提交消息的 subject 和 body 必须使用英文。

### 格式

```text
<type>(<scope>): <short summary>

- <change point 1>
- <change point 2>
```

正文使用 `-` 开头的 bullet list。

### 类型（type）

优先使用：`feat`、`fix`、`refactor`、`perf`、`test`、`docs`、`build`、`ci`、`chore`、`revert`。
无合适匹配时默认 `chore`，不自行发明新类型。

仅在以下情况允许使用列表外的 type：

- 用户明确要求
- 仓库历史中已经形成稳定惯例

使用列表外的 type 时，在正文中添加一条简短的理由说明。

### 范围（scope）

scope 按模块优先、目录其次的顺序选择：

- 变更主要位于一个模块时，使用该模块名（如 `data-ingestion`、`container-management`）
- 变更跨多个模块但服务于同一功能链路时，使用功能性 scope
- 变更跨多个无关模块且没有统一主题时，使用 `multi`

scope 必须使用小写 kebab-case。

### 正文

- 用户明确指定提交范围时，仅根据该范围生成提交消息
- 用户未指定提交范围时，根据当前变更生成提交消息
- 正文只总结选定提交范围内的修改，无论相关修改处于暂存区还是工作区
- 每条 bullet 描述一个独立变更点，不重复
- 仅描述实际代码/内容变更，不包含对话反馈、review 意见等过程性描述

### 反引号处理

提交消息含反引号时，使用单引号 `-m '...'` 或 `git commit -F` 传递，禁止使用双引号 `-m "..."`（避免 shell 命令替换）。

## 提交前检查

### 1. 敏感信息

同时检查暂存文件列表和暂存区 diff，确认其中不包含密码、私钥、access token、API key、凭据、个人信息、用户公网 IP、环境变量文件或值等敏感信息。

发现敏感信息时：

- 停止提交并将其排除在暂存变更之外
- 告知用户发现了敏感信息，但不在输出中复述敏感值
- 只有用户明确确认该具体内容应公开并纳入版本控制后，才允许提交

### 2. 格式化

对提交范围内的文件进行格式化：

- 优先使用仓库内已定义的格式化规则、命令、脚本、hook 或配置文件。
- 如果仓库未定义，使用该语言或文件格式的官方格式化工具或风格。
- 如果不存在官方标准，遵循广泛接受的行业惯例。
- 格式化范围限定在提交范围内的文件，不引入无关的全仓库变更。
- 检查格式化结果，并重新暂存由格式化产生的范围内变更。
- 格式化工具不可用、配置冲突、或格式化可能带来风险时，停止并告知用户，不跳过检查、不自行安装工具。

### 3. Lint

对提交范围内的文件进行检查：

- 运行提交范围内所有适用的仓库 lint 规则、命令、脚本、hook 或配置文件。
- 如果仓库未定义，使用该语言或文件格式的官方 linter。
- 如果不存在官方 linter，使用广泛接受的 linter 或行业惯例。
- 先格式化后 lint（除非仓库明确定义了不同顺序）。格式化与 lint 是独立要求，一项通过不代表另一项可跳过。
- `git diff --check` 等空白检查不能替代格式化或 lint。
- lint 范围和自动修复限定在提交范围内的文件，不引入无关的全仓库变更。
- 检查自动修复结果，并重新暂存由 lint 修复产生的范围内变更。
- Linter 不可用、配置冲突、需要安装工具、或修复可能带来风险时，停止并告知用户，不跳过检查、不削弱规则、不自行安装工具。
- 提交前报告实际执行的每条 lint 命令及是否通过。如果经检查仓库配置和常规工具后仍不存在可用 linter，明确报告无可用的 lint 命令，并停止等待用户指示。

### 4. 临时文件

`TEMP_ROOT` 固定为仓库根目录下的 `.agent-tmp/`，不接受用户覆盖。

#### 路径安全

- `TEMP_ROOT` 不得是符号链接
- `TEMP_ROOT` 的规范路径必须位于当前仓库根目录内
- 临时文件优先放在 `TEMP_ROOT` 下的任务专用子目录中，避免任务间冲突

#### 文件追踪

创建文件时，将其分类为：

- `temporary`：任务完成后可以安全删除的临时文件
- `retained`：需要保留的有用输出

维护当前会话的文件追踪列表，并为每个 Agent 创建的文件记录：

- 绝对路径
- 分类（`temporary` 或 `retained`）
- 简短用途说明

追踪列表不可用、过期或不完整时，停止自动清理并要求用户明确批准删除。

#### 自动清理

任务完成后，只自动删除同时满足以下条件的文件：

- 位于 `TEMP_ROOT` 下
- 已记录在当前会话追踪列表中
- 分类为 `temporary`

清理时还必须遵守：

- `retained` 文件未经用户明确要求不得删除
- 正常程序运行、构建、测试或运行时流程产生的文件未经用户明确要求不得删除
- 删除前检查目标的规范路径仍位于 `TEMP_ROOT` 下，并在执行删除命令前再次校验
- 目标规范路径不在 `TEMP_ROOT` 下时，按目录外临时文件处理
- 未经用户明确批准，不删除 `TEMP_ROOT` 之外的文件
- 仅当通配符或递归删除展开后的全部目标都是已追踪的 `temporary` 文件时，才允许执行
- 展开结果包含未追踪文件或 `retained` 文件时，停止删除并列出精确路径，请求用户批准

#### 目录外临时文件

临时文件必须优先创建在 `TEMP_ROOT` 中。确需在目录外创建时，记录其绝对路径和用途。

删除目录外临时文件必须获得用户明确批准，并提供：

- 精确的绝对路径列表
- 精确的删除命令
- 批量批准全部文件的选项
- 逐个批准文件的选项
- 取消且不删除的选项

用户未明确选择时，默认取消删除。

#### 提交前临时文件检查

- 临时文件不得纳入提交
- 提交前检查暂存区和 tracked changes，确认不包含 Agent 创建的临时文件
